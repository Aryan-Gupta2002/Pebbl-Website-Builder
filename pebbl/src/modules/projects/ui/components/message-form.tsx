import { useForm} from "react-hook-form"

interface Props {
    projectId:string;
};

export const MessageForm=({projectId}:Props)=>{
    return (
        <div>
            Message form
        </div>
    )
}